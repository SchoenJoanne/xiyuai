# 敏感配置
.env
.env.local
.env.*.local
.auth-secret
.admin-secret
.admin-credentials
.weixin-credentials.json
*.secret

# 运行时数据
data/
logs/
# v1.9.1: ai_taste_scan 输出含敏感对话内容
reports/
*.db
*.db-wal
*.db-shm

# 用户上传内容
public/avatars/*
!public/avatars/preset/
!public/avatars/.gitkeep
!public/avatars/placeholder_neutral.svg
public/assets/scenes/

# 依赖
node_modules/
package-lock.json.bak

# 系统
.DS_Store
Thumbs.db
*.log

# 编辑器
.vscode/
.idea/
*.swp
*~

# 备份
_backup_*
backups/
*.tar.gz

# ─── explicit sensitive runtime data ────────────────────────────────────────
data/bot.db*
data/user_memories/
data/uploads/
data/*.sqlite
data/*.sqlite3

# ─── production / private backup archives ──────────────────────────────────
_backup_billing_v1/
*.tar
*.zip

# ─── generated private media ───────────────────────────────────────────────
public/avatars/scenes/
public/assets/scenes/
public/uploads/
public/generated/

# ─── 表情包素材（商用授权·不开源·🔴 绝不进 git）─────────────────────────────
# app 从 assets/stickers/ 加载（manifest.json + 按 tag 的图片子目录）。
# 真实图片与 manifest.json 一律忽略；只保留加载机制说明 README 与示例 manifest。
assets/stickers/*
!assets/stickers/README.md
!assets/stickers/manifest.example.json
!assets/stickers/PROVENANCE.md
!assets/stickers/.gitkeep
# 🔴 将来若放行 CC0 原创集：加 `!assets/stickers/xiyu2/` 一行（zh/* 仍被上面 * 挡住）。
#    opensource_check.sh 第 7 项会拦"非 xiyu2 图片被跟踪"——机制侧已就位。

# ─── local WeChat / iLink credentials (kept here for visibility) ───────────
# (also covered by line 8 above)
.weixin-credentials.json

# ─── provider pricing config (private) ──────────────────────────────────────
config/provider_pricing.json
__pycache__/
scripts/_ab_out/

# v1.21.4: 标注语料导出（含用户对话原文，绝不进 git）
data/exports/
**/exports/*.jsonl

# 发布副本 PII 黑名单(闸14 读取·含真实姓名/私邮·绝不进公开仓/tracked)
scripts/.release_pii_blacklist
